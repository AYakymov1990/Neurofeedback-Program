# Neurofeedback Project (BrainFlow + Matplotlib)

Launch:

1) Create and activate venv
2) Install dependencies
3) Run `main.py`

Example:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py
```


